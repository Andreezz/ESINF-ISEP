/*package Exame2022E;

public class Ex5 {
    public static void main(String[] args) {

    }

    private void preOrder(int index, List<V> snapshot) {
        snapshot.add(heap.get(index).getValue);
        if (left(index) < heap.size()) {
            if (heap.get(left(index))!=null) preOrder(left(index),snapshot);
        }
        if(right(index<heap.size)){
            if (heap.get(right(index))!=null) preOrder(right(index),snapshot);
        }
    }
}*/
