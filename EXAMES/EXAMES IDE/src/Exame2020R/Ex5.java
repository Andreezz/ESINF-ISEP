package Exame2020R;
/*
import java.util.LinkedList;
import java.util.Map;

public class Ex5 {
    public static void main(String[] args) {

    }

    public List<V> getElemsLevel(int level) {
        int n = 2;
        double i = 0;
        int max=(int)Math.pow(2,level);
        while (n >= 0) {
            i = i + Math.pow(2, n);
            n--;
        }
        n-=1;
        max+=n;
        List<V values=new LinkedList<>();
        for (Entry<K, V> e : heap) {
            if (e.getKey()>n&&e.getKey<=max){
                values.add(e.getValue());
            }
        }
        return values;
    }

}
*/