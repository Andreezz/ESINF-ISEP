package Exame2021N;

public class Ex2 {
    // A complexidade temporal deste metodo e O(log n), o if tem de complexidade O(1), o loop tem de complexidade O(log n) o loop e iterado pela variavel i que por sua vez aumenta em 2 vezes em cada iteracao,
    // ou seja para cehgarmos a um valor maior que n necessiatmos de log(n) iteracoes.
    //A linha seguinte tem de complexidade O(1) e a ultima tambem, logo temos que a complexidade temporal e O(log(n)).
}
